# Stage.ini 写入错误与修复结论

## 2026-07-16 无资源发布任务初始结论

- 当前分支为 `codex/resource-free-editor-release`，分支起点提交 `231a255` 只加入实施计划，尚未实现无资源发布链路。
- 当前发布方案明确允许默认采用系统临时会话目录；调色板策略在未确认时保持现状，不能自行移除或替换颜色数据源。
- 工作区唯一既有未提交项是未跟踪的 `data/game-cjsh/`，属于用户本地数据，必须与代码和发布产物隔离。
- 根目录 `task_plan.md`、`findings.md`、`progress.md` 是既有持久规划文件，本任务在其中追加独立章节，不删除历史维护记录。
- `build_editor_release.py` 当前仍无条件调用 `export_editor_bundle`，以 `stage01` 为默认关卡，并把完整 Bundle 复制到 `editor-data`。
- `editor_desktop_launcher.py` 当前要求 `index.html` 与 `index.json` 同时存在，默认尝试打开 `stage01/editor.html`，没有资源选择或会话生命周期。
- `export_editor_bundle.py` 当前仍导入 `find_text_data_dir`，并直接从游戏目录读取 `heads.dat`、`kingdom.cel` 等文件生成派生资源。
- 当前测试只覆盖旧发布数据目录、启动器服务器和关卡 Bundle 元数据，尚无无资源白名单、禁用扫描、运行时会话或干净构建测试。
- 运行时可复用现有 Bundle 生成能力，但必须新增显式源目录入口；仅把用户目录当作 `root` 仍不足以隔离数据，因为文本目录查找会继续回退到仓库 `data/text`。
- 当前 `copy_scenario_reference_files` 会把用户的 `.dor/.stg/heads.dat/History.txt/stage.ini` 复制进 Bundle；该行为只能保留在临时会话，不能出现在发布包。
- 当前 Sidecar 元数据在缺少 `.s/.x` 时明确允许尾区零填充，与实施计划的完整导出要求冲突，运行时导入必须把两者列为完整导出所需输入。
- 当前网页初始化固定请求同目录的 `stage.json` 和资源目录，并额外请求上级 `index.json`；无资源根页面必须使用独立静态入口，已加载会话才进入该模板。
- PyInstaller 当前从启动器递归收集模块；新增运行时会话模块由启动器显式导入即可被收集，package data 仍只需 HTML 与 KSY。
- `build_editor_common_model` 的有效数据只来自显式游戏目录中的 `stage.ini`、`History.txt` 与可选 `general.txt`，可改为显式目录调用而无需文本母表。
- 完整据点联动分析会经过多个文本数据管线；运行时应直接使用已有的 `.dor/.stg` 解析回退函数，避免间接触发仓库文本目录。
- `build_stage_ini_patch_model` 是剩余的 `data/text` 强依赖；运行时可明确标记预生成模型不可用，并由网页现有的本地 `stage.ini` 块流解析器恢复字段与追加布局。
- 网页已具备多文件本地项目导入能力，可作为已加载会话中的重新导入入口；需要补充替换确认和缺失参考文件提示。
- 发布审计采用五个精确白名单文件：EXE、空入口 HTML、发布元数据、短说明和完整用户指南；未列入的普通 UI 图片同样拒绝。
- 目录与 ZIP 审计均拒绝路径穿越、盘符路径、重复条目、关卡目录、原始扩展名和计划列出的派生资源文件名。
- 审计返回按 POSIX 相对路径排序的路径、字节数和 SHA-256，可直接写入外部构建清单。
- 第 2 阶段的 13 项发布审计、启动器与构建元数据测试全部通过。
- 正式发布构建已完全移除 export_editor_bundle 导入与 --stage 参数，只写空入口、release-info、EXE 和两份指南。
- 发布元数据明确标注 user-import-only、system-temp 与调色板待确认策略，不再含默认关卡。
- PyInstaller TOC 审计拒绝 data/game、data/text 与禁用游戏文件；目录和 ZIP 清单必须逐项同哈希。
- 真实构建生成 5 文件 ZIP，大小 12,622,781 字节，SHA-256 为 e61b051ff8278d0dffc5220df81f6d6f8279714d3101eda23a72e6ab75d0d8b5。
- 完整自动测试 104 项通过，1 项按样本条件跳过；正式 EXE 的无资源数据目录检查在沙箱外通过。
- 运行时输入严格要求当前 .m、kingdom.cel、同名 .dor/.stg/.s/.x、stage.ini、History.txt 和 heads.dat；kingdom.atr 为可选项。
- 校验报告记录识别文件的绝对来源、用途、大小与 SHA-256，并分别展示缺失、重复、场景编号错配、错误和警告。
- 临时会话只在显式会话根的直接子目录创建，只有带专用标记的目录可被自动删除，路径越界会被拒绝。
- 新会话完整导出成功后才替换旧会话；失败会清理半成品并保留旧会话，输入路径和哈希未变化时复用当前会话。
- 过期会话在下次启动时可按标记时间回收，正常关闭幂等清理当前会话。
- 阶段 4 完整回归 115 项通过，1 项按样本条件跳过。
- 运行时导出入口从校验报告取得显式 source_dir 与 .m 路径，不调用 find_game_dir 或 find_text_data_dir。
- 地图底图、三层资源目录、缩略图、小地图和 heads.png 均在临时会话导入后生成，发布包不预生成或复制这些文件。
- 运行时据点联动直接解析用户 .dor/.stg，不进入依赖文本数据的完整分析管线。
- 用户 .dor/.stg/stage.ini/History.txt/heads.dat 只复制到临时会话；.s/.x 原始文件不复制，合法尾区写入 stage.json.sidecars。
- 缺少任一 .s/.x 时运行时导出拒绝继续，不再允许尾区零填充。
- 当前阶段明确把 stageIniPatchModel 标为不可用，避免回退 data/text；下一阶段将用内置格式字段定义恢复。
- 真实 stage01 集成和完整回归共 116 项通过，1 项按样本条件跳过。


## 样本

- 游戏有效文件：`data/game/stage.ini`，75,280 字节，SHA-256 `29584DE26770323A09849D180331D936E9C112F55936D76B08F4F6F6A63663B8`。
- 首次崩溃样本（当时的 `outputs/stage.ini`）：75,880 字节，SHA-256 `A2026A0BFD66F95E0F6F7B661B5A871C5E00CB065BB7AC2833878C36894CA948`。
- 首次块边界修复样本：75,608 字节，SHA-256 `F64737E1A6A0CF01F2B94118132B471E04C2131FF7121718371BF0C1C0BDE3F3`。
- 当前人物 ID 对齐输出：75,836 字节，SHA-256 `2038FDF997EE042D6A35AA63247504AD220D3CA1A5EA34368CAFBF5F438AC540`。
- 文件是含 NUL 的二进制母表，不是文本 INI；新增标题可按 CP950/Big5 正确解码，编码不是本次崩溃根因。

## 真实物理结构

- 文件以 `main_count:u4` 开始，随后是 `main_count` 个 `size:u4 + payload[size]` 主块。有效样本有 277 个主块，`size` 均为 224，主块流结束于 63160。
- 前 272 个主块是普通武将：20 字节标题/类型槽、49 个命名 DWORD、2 个能力镜像 DWORD；加上 size 前缀后每条共 228 字节。
- 偏移 63160 是 `city_count=42`。每座城池由 `size=92` 主块和 `size=0` 次块组成：主块内为 1 个前导 DWORD、20 字节标题/类型槽、17 个命名 DWORD；每座共 100 字节。
- 42 座城池结束于 67364。此前把 67364、67368 的两个 `76` 归入“南海”尾字段是错误的；它们属于城池区之后的下一资源块。
- 旧的“8 字节头 + 224 字节主视图 + 76 字节尾视图”只是一种可字节一致回写的兼容工作簿视图，不是物理记录格式。

## 根因

- `_stage_ini_append_layout` 根据相邻命名数值流反推标题和行长，错误得到 32 字节标题槽，并在最后命名字段后直接插入。
- 武将从 62012 插入会覆盖最后一名普通武将的两个能力镜像；62020 是下一个合法块边界，但人物 ID 278/279 仍不能放在这里。新增武将必须追加到全部原主块结束位置 63160。
- 城池因此从 67360 插入，覆盖最后一座原城池的 `size=0` 次块；正确插入点是 67364，不是初步分析中的 67372。
- 前端又把 228 字节武将行补齐为 448 字节、100 字节城池行补齐为 152 字节，净增 600 字节，并只按伪主步长重算一个计数。

## 修复

- 新增真实块流解析器，输出每个块的 size、payload 与结束偏移。
- 新增武将写入完整 228 字节块，保留标题类型标记，并从“最大武力/最大智力”写入两个尾随镜像。
- 新增城池写入完整 100 字节双块，保留前导值、标题类型标记和零长度次块。
- 主块数和城池数分别在位移后的真实偏移更新，不再执行 224/76 步长补齐。
- 首次修复样本曾从游戏有效母表重建并迁移武将“劉飛羽”和城池“北都”；当前 `outputs/stage.ini` 已是后续新增两名武将和“晉都”的新样本。

## 2026-07-14 人物 ID 对齐补充

- 有效母表的物理第 273..277 块依次是大魔王、女武神、白吉勝、田恩沛、黃柏文，内部人物编号也为 273..277。
- 上一轮把新增块插在 62020，只解决了块边界和崩溃问题，却把新 ID 278/279 放到了物理第 273/274 块，并把原 276/277 推到物理第 278/279 块。
- `.stg` 引用 278/279 时，游戏按主块 1-based 物理序号取到田恩沛、黃柏文，因此名称与编辑器不一致。
- 新增武将的正确插入点是原全部主块结束及城池计数偏移 63160。插入后新 ID 278/279 分别处于物理第 278/279 块。

## 2026-07-14 完整项目快照导入重复城门

- `outputs/stage01.dor` 实际只含四个新增的晉都城门；出现八个城门不是 `.dor` 重复写入。
- 原导入流程先读取本地 `.dor`，再把同一批导出的 `stage01_dor_patch.json` 当作增量 Patch 再应用一次，因此四个城门被重复登记。
- 只选择 `.m/.dor/.stg/.ini/.s/.x/History.txt` 时，浏览器端不会把本地 `.stg` 重新解析为据点、武将管理模型；新增对象仍在二进制中，但 UI 没有可显示的场景快照。
- 修复后，当 `.m` 与对应本地二进制同时载入时，`scenario/dor/history` JSON 仅用于恢复管理 UI 快照，并清空对应 Patch 集合；单独导入 JSON 仍按跨版本 Patch 执行。

## 2026-07-14 本地 STG 二进制导入

- 原导入流程只保存本地 `.stg` 字节，管理 UI 仍沿用内置 bundle 的旧场景模型，因此新增的晉都和武将在二进制中存在但不可见。
- 浏览器现按 `stg.ksy` 已确认的块流顺序解析 root、force、site、primary Entity 和五类可选 Entity，并同步生成字段 Patch 偏移与对象重建布局。
- 当前 `outputs/stage01.stg` 可解析出劉備势力下的晉都，以及人物 ID 278 的劉飛羽；随后 `.dor` 按解析后的据点坐标关联，不再依赖旧据点表。

## 2026-07-14 本地 stage.ini 母表恢复

- 本地 `.stg` 已能恢复人物 ID 278 的 Entity，但 ini 页此前仍查询内置 bundle 的 272 行普通武将工作表，因此显示“未匹配到 stage.ini 武将母表行”。
- 浏览器现顺序读取本地 `stage.ini` 的全部 `size=224` 主块和 `size=92 + size=0` 城池双块，按 1-based 物理序号建立母表行。
- 每个武将恢复 49 个数值字段及字节偏移，每个城池恢复 17 个数值字段及字节偏移；导入后的主块结束、城池计数和城池结束位置成为新的追加边界。
- 当前 `outputs/stage.ini` 验证为 278 个主块、43 个城池，物理第 278 行标题为“劉飛羽”且人物编号为 278。

## 2026-07-14 本地 History.txt 恢复

- `outputs/History.txt` 使用 CP950 TSV，确实包含“劉飛羽 / 278 / 加入年 189”记录。
- 原导入流程只保存本地 History 字节，右侧 History 页仍读取内置旧表，因此人物 278 无法匹配。
- 浏览器现保留带空格的原始表头，同时生成规整字段键和以人物编号为 `rowKey` 的记录；再与本地 `stage.ini` 属性合并候选武将。
- 本地文件中的 CP950 单字节和双字节映射会并入导出字符表，避免新增文本在再次导出时无法编码。

## 2026-07-14 小地图颜色自动派生

- `acwx/acwy/acwz` 与 `minimap_color` 不是一一映射；全量 xyz 的样本内众数命中率为 95.091%，但确定记录只占 26.765%。
- 首版采用 `xyz→xy→xz→x→全局众数` 回退，33 关留一验证准确率为 83.618%；改为 `z→y→x` 优先的全组合顺序后提升到 94.564%。
- 编辑器只在 xyz 真实变化且同批没有显式颜色时派生，不重算导入地图；Raw 手工值优先，恢复操作可精确回到原始颜色。

## 2026-07-14 新增武将默认值调查

- 工作区在本任务开始前已有 `editor_app.html` 文案修改：“已编辑绿框”改为“已编辑框”；该修改需保留但不纳入本任务提交。
- 新增武将至少有两个相关流程：武将管理创建 `stage.ini` 母表候选，以及据点管理把已有候选加入 `.stg` Entity；需要确认二者是否共享字段默认值。

- `addScenarioEntity` 当前先把 `general` 工作表全部列初始化为 0，只单独设置人物编号、头像编号和统御力 1；这就是新增母表武将的现有默认值来源。
- `addExistingGeneralToSite` 会从母表候选复制等级、带兵、统御、武力、智力、忠诚和三项最大值到新 `.stg Entity`，因此母表字段映射和默认值必须同时完整，才能保证加入据点后不丢值。
- `addScenarioSite` 优先取尚未被场景占用的 `stage.ini` 城池行；否则按“房屋属性 0 且城规模 1、房屋属性 0、任意首行”的顺序选模板，并复制整行 `stageIniValues`。
- 新据点显式字段从种子或模板继承；无模板时房屋属性为 0、城池规模为 1，其余经济和管理字段为 0。坐标取当前 Cell，归属取当前势力，都市索引与母表行号取现有最大值加一。

- `STAGE_INI_FIELD_MAP.entity.fields` 当前只映射到忠诚值，缺少 `max_troop_count/max_martial_force/max_intellect`；必须补入“最大帶兵數/最大武力/最大智力”，新母表候选才能把三项最大值转换为通用属性并传给新增 Entity。
- `.stg` Patch 索引本身已包含三项最大值，因此新 Entity 行只要带有对应属性，重建时就会覆盖模板值；无需改变 KSY 或二进制偏移。
- 本地 `.stg` 浏览器解析器目前只恢复到忠诚值；已有 Entity 的未知后续字段仍由原始块保留。本任务只改变新建对象，不重写既有对象默认值。

## 2026-07-14 新增据点固定默认值

- `64817cf` 已包含新增武将默认值，但当前 worktree 是 detached HEAD，`master` 和其他分支均未引用该提交。
- 新据点继续复用可用母表行的身份字段，避免无必要追加；业务数值不再继承种子或模板。
- 新母表行全部数值列先清零，再写入固定业务初值、都市索引和当前 Cell 坐标；历史 Patch 的模板补齐保持兼容。
