# 项目历史

本文档只保留会影响项目方向、数据模型或可用能力的里程碑，不记录逐次命令和临时调试过程。当前行为应以 README、技术文档、源码和测试为准。

## 2026-06-23 至 2026-07-02：基础格式与编辑闭环

- 确认 `.m` 文件头、16 字节 Cell 记录和 `kingdom.cel/.atr` 地图资源渲染方式。
- 完成地图浏览、基础图层编辑、Patch 导出及 `.m/.s/.x` 保守写回。
- 建立 `stage.ini`、`.stg`、`.dor` 的结构化分析与工作簿转换管线。
- 建立 `.dor` 城门与 `.stg` 据点的坐标关联，编辑器可以定位并高亮据点和城门。

## 2026-07-03 至 2026-07-05：编辑器 2.0 架构

- 界面从底层字段分栏转为左侧资源、中央地图、右侧管理与底部状态的任务导向布局。
- 确立原始层、领域层、视图层的运行时分工，Raw 字段能力保留但不再占据主工作区。
- 补齐“游戏文件 -> 中间结构 -> 转换入口 -> 回写入口”的维护链路。

## 2026-07-08：STG 块流模型修订

- 把 `.stg` 主结论从早期固定 76 字节视图修正为 Emperor.exe 风格的多级 `u32 size + payload` 对象流。
- 使用 42 个 `.stg` 样本验证对象边界和 byte-for-byte roundtrip。
- 确认 `entity_part1 +0x00..+0x20` 是固定零保留区，修正旧的所属势力解释。
- 确认 `site_part2 +0x27C..+0x28C` 是五个可选 Entity 控制位，`+0x2AC` 在当前样本中固定为零。
- 未完全命名的运行态字段和尾区继续原样保留；并行 STG 研究实现的全面收口暂时搁置。

## 2026-07-09 至 2026-07-13：地图编辑器 2.0 可用版本

- 完成势力、据点、城门、武将和士兵的管理与关联刷新。
- 完成武将头像、History 候选武将、据点分类和多标签字段编辑。
- 完成图层叠加、中心点标记、坐标搜索、撤销重做、区域复制剪切和合成对象。
- 完成新增城池、武将及 `stage.ini/.stg/.dor/History.txt` 的关联写回和 ZIP 导出。
- 增加 Patch 跨版本导入、冲突校验、Raw 一键恢复和小地图本地重建。
- 完成 Windows 发布包入口、创建者与打包日期信息。

## 2026-07-14：stage.ini 块流写入修订

- 对比游戏有效母表与崩溃输出，确认旧实现把兼容工作簿的 224/76 字节视图误作物理步长。
- 确认普通武将为 `size=224` 的 228 字节完整块，城池为 `size=92` 主块加 `size=0` 次块的 100 字节双块。
- 修正新增对象插入边界、标题类型槽、武将能力镜像和两级计数更新，并用真实游戏母表锁定回归边界。
- 进一步确认游戏以主块物理序号解析人物 ID；新增武将必须位于全部 277 个原主块之后，不能插在 ID 273..277 的特殊武将之前。

## 2026-07-13：GitHub 发布前整理

- 复核 `master` 历史不存在超过 1 MiB 的文件，无需重建 Git 仓库。
- 统一本地资源目录为 `data/game` 与 `data/text`，清除源码和测试中的本机绝对路径。
- 移除旧 `tools` 转发层、硬编码 Node 脚本、生成分析文件和失效测试。
- 补齐 package data、CI、许可证、贡献指南、安全策略和 GitHub 模板。
- wheel 已验证包含 `editor_app.html` 与 `.m/.dor/.stg` 三份 KSY。
- 本地完整测试结果为 70 项通过，1 项因未准备 `stage11` 样本而跳过；无游戏素材环境中集成测试会明确跳过。

## 2026-07-14：命令入口与手册补全

- 复核旧 `tools` 转发目录后确认四个可执行入口未迁入统一注册表，补齐资源分析、STG 家族分析、文本双向转换和 `.m` 图层导出命令。
- 将 Windows 发布构建、桌面启动和小地图颜色关系分析纳入统一入口。
- 修复 `stitch-kingdom` 与 `export-m-layers` 的旧顶层导入，建立全部注册命令的帮助可用性测试。
- 新增完整命令执行参考，统一说明参数、默认产物、兼容工具边界和写回风险。
- 完整测试结果为 72 项通过，1 项因未准备 `stage11` 样本而跳过。

## 2026-07-14：完整项目导入快照修订

- 将配套二进制与导出 `scenario/dor/history` JSON 的同时导入定义为“项目快照恢复”：JSON 只恢复管理界面，避免把已写入 `.stg/.dor/History.txt` 的记录再应用一次。
- 保留单独 JSON 的跨版本 Patch 语义；二进制单独导入暂不解析本地 `.stg` 为新增管理对象，完整项目需同步选择导出的 JSON 快照。
